<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VolunteerTracker">
        <activity android:name=".create_account_activity"/>
        <activity android:name=".home_activity"/>
        <activity android:name=".admin_profile_activity"/>
        <activity android:name=".profile_activity"/>
        <activity android:name=".admin_dashboard_activity"/>
        <activity android:name=".volunteer_goal_activity"/>
        <activity android:name=".choose_org_activity"/>
        <activity
            android:name=".main_activity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>